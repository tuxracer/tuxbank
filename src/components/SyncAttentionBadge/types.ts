/** Badge content for a sync status that needs the user's attention. */
export type SyncAttention = {
  label: string;
  /** CSS custom-property reference from the cyberpunk palette. */
  colorVar: string;
};
