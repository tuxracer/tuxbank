/**
 * Fallback compact threshold used when the --breakpoint-sm token cannot be
 * read (stylesheet not applied yet, jsdom). Matches Tailwind's default `sm`.
 */
export const COMPACT_FALLBACK_BREAKPOINT = "640px";
